output {
  elasticsearch {
<% if scope.lookupvar('logstash::config::elasticsearch_provider') == 'embedded' %>
    embedded => true 
<% else %>
    host => "<%=scope.lookupvar('logstash::config::elasticsearch_host')%>"
<% end %>
  }
}
