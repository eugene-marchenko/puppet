<% 
  users = []

  if @vsftpd_users_list
    if @vsftpd_users_list.kind_of?(Array)
      users = @vsftpd_users_list
    else
      if @vsftpd_users_list =~ /, /
        users = @vsftpd_users_list.split(', ')
      elsif @vsftpd_users_list =~ /,/
        users = @vsftpd_users_list.split(',')
      else
        users = @vsftpd_users_list.split
      end
    end
  end
-%>
<% users.each do |user| -%>
<%= user %>
<% end -%>
